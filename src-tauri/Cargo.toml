[package]
name = "brainroots-cards"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = { version = "2" }

[build-dependencies]
tauri-build = { version = "2", features = [] }
